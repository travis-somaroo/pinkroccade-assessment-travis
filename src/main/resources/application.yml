spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pinkroccade_db
    username: sa
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
